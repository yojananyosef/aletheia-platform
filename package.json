{
  "name": "aletheia-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma biblica universal gratuita y abierta (Logos-like) — Expo + modulos .amod",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.4.0",
  "scripts": {
    "typecheck": "tsc -b && tsc --noEmit -p apps/mobile",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
